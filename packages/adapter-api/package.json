{
  "name": "dsh-magic-context-adapter",
  "version": "0.1.1",
  "type": "module",
  "description": "Magic Context adapter-api: stable Host-neutral facade over the shared Magic Context core for harness adapters (initial: type + contract surface only, per PLAN §2).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist",
    "src",
    "README.md",
    "THIRD_PARTY_NOTICES.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@magic-context/core": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^22.20.0",
    "typescript": "^5.8.0"
  }
}
